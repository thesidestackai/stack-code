# Disposable-workspace fixture: OK

Carries the marker file at `.claw/harness-disposable.marker`. Used by
the harness adapter classifier tests to exercise the all-signals-pass
path.

This is a static read-only fixture. No test code writes into it.
